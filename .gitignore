# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

#avante project description
avante.md

.claude/*

TODO.txt
/bindings/
coverage/

# Design documents — kept locally, not tracked in the repo.
docs/*.md

# Local Flatpak build artifacts (flatpak/build-local.sh).
# The build tree + OSTree repo now stage in ~/.cache/clai-flatpak (outside
# the repo); only the final bundle lands here. The dir entries are kept as
# defense against stale trees / running the CI-style steps in-tree by hand.
/clai.flatpak
/repo/
/flatpak-build/
